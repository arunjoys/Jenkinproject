version: '3'
services:
  Frontend:
    image: drupal
    ports:
    - 80:80
    deploy:
      replicas: 6
    networks:
    - jenkinet
  Backend:
    image: postgres
    deploy:
      replicas: 6
    environment:
    - POSTGRES_PASSWORD=123456
    networks:
    - jenkinet
networks:
  jenkinet:
